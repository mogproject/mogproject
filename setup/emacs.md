Setup Emacs
====

```
## deprecated ## brew install emacs --with-cocoa
brew install --cask emacs
## deprecated ## git clone git@github.com:mogproject/emacs.d.git ~/.emacs.d
ln -s ~/Dropbox/config/_emacs.d ~/.emacs.d
```

